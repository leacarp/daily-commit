name: Daily Green Commit

on:
  schedule:
    - cron: "0 13 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Modify file
        run: |
          echo "Commit automático $(date)" >> activity.log

      - name: Commit changes
        run: |
          git config user.name "leacarp"
          git config user.email "leanaguirre2016@gmail.com"
          git add .
          git commit -m "chore: daily auto commit" || echo "No changes"

      - name: Push changes
        run: git push
